/** Invariant companion plugin (no assertions — nothing to check at runtime). */

/** Provides no assertions: the tool writes no durable session events — the image never enters the conversation. */
export function apply(): void {}
