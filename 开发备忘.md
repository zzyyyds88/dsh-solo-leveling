# 开发备忘（DSH我独自升级）

> 工作区正式规矩见 [CONTRIBUTING.md](CONTRIBUTING.md)（人类 + AI 通用）与
> [AGENTS.md](AGENTS.md)（AI 代理强制红线）。

## GitHub 加速下载

从 GitHub 拉取代码时，一律使用加速地址：

```
https://git.greatbaby.cc.cd/
```

用法示例（替代 `github.com`）：

```bash
# 下载 tarball（推荐，git clone 在直连时容易超时）
curl -L -o repo.tar.gz "https://git.greatbaby.cc.cd/https://github.com/<owner>/<repo>/archive/refs/heads/main.tar.gz"

# 或 git clone 走加速
git clone "https://git.greatbaby.cc.cd/https://github.com/<owner>/<repo>.git"
```

已用此方式成功下载：
- dsh-web-ui（zhu1090093659/dsh-web-ui）→ <仓库根目录>/dsh-web-ui/（快照已删除，需要时按上述命令重下）

## 原则（用户明确要求）

- **不要直接修改 node_modules 里的插件打包产物**（lib/*.js），会改坏且无法维护
- 插件改造必须基于**源码**（src/）→ 重新构建 → 再安装
- 插件源码保存在本地后，**由用户按自己的想法改造**，AI 不直接动插件文件
- 需要改造时，先向用户确认改造方案再动手

## Linux 测试环境注意事项（2026-08-23 实测）

本机（Ubuntu 22.04，SSH 登录）全量 `pnpm test` 曾有 12 个失败，逐一定位后全部是
环境因素而非代码问题。按下面方式跑即全绿（real-product 除外，见末条）：

1. **root 权限类（7 处失败）**：settings-file×2、bash-sandbox 的 chmod 用例、
   sqlite-backend、out-of-process、agent-instructions、subagent-acp。
   这些用例靠「chmod 掉权限→期望报错」；root 绕过权限位必挂。
   → 用非 root 用户跑：`su dev -s /bin/bash -c 'cd /root/DSH && npx vitest run <files>'`
   （已建 dev 用户；/root 已给 dev 遍历 ACL；工作区已挂 dev 读写默认 ACL）
2. **bash BASH_ENV 类（1 处失败）**：bash-sandbox 的「BASH_ENV 求值顺序」用例。
   Debian 系 bash 编译带 SSH_SOURCE_BASHRC：环境含 `SSH_CLIENT` 时，非交互 bash
   改读 ~/.bashrc 而跳过 $BASH_ENV。SSH 登录的会话必带该变量。
   → 跑测试前清掉：`env -u SSH_CLIENT -u SSH_CONNECTION -u SSH_TTY npx vitest …`
3. **git 版本（1 处失败）**：install-lefthook.spec 依赖「`git worktree add` 会把
   core.hooksPath 拷进 worktree 配置」的行为，git 2.34 没有。已通过
   ppa:git-core/ppa 升到 2.55 解决。
4. **real-product（3 处失败，暂无解）**：subagent-claude-code 的真产品用例要拉起
   Claude Code CLI 并真实对话，需要 Anthropic 凭据（登录态或 ANTHROPIC_API_KEY）。
   配好凭据即可过；否则保持失败属预期。

推荐组合命令（验证这批文件）：

```bash
su dev -s /bin/bash -c 'cd /root/DSH && env -u SSH_CLIENT -u SSH_CONNECTION -u SSH_TTY npx vitest run <files>'
```
