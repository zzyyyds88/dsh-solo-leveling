{
  "// purpose": "Self-contained compiler settings for the consumer-side `prepare` build (git installs). The repo tsconfig extends the sibling harness checkout, which does not exist on a consumer machine; type checking stays a dev/CI concern (`pnpm run typecheck`).",
  "compilerOptions": {
    "target": "ES2024",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src"]
}
