{
  // Solution file: one program per side (host half vs browser half), the
  // main-repo rule for packages that span both — the host `sessions:
  // SessionStore` merge (dsh-session root) and the browser `sessions:
  // ISessions` merge (client runtime) cannot share a ts.Program (TS2717).
  "files": [],
  "references": [
    { "path": "./tsconfig.host.json" },
    { "path": "./tsconfig.client.json" }
  ]
}
