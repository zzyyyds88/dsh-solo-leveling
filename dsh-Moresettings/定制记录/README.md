# dsh-defaults 定制记录

- 变更记录.md —— 本次改动的完整记录（做了什么、为什么、怎么验证）
- 本次diff.patch —— 相对上游源码的 fork 改动 diff（升级后重打参考）

## 项目来源

本插件合并了工作区两个历史项目（均已并入本插件并删除原文件夹）：

| 原项目 | 去向 |
|---|---|
| 修改默认工作目录（已删除） | 默认工作目录改为设置页配置（host picker fork 读取） |
| 思考强度与重试默认值（已删除） | 思考强度保持 fork 内建默认；重试默认次数改为设置页配置，对所有供应商生效 |
