{
  "name": "@deepseek-ai/dsh-llm",
  "version": "0.1.0-rc.7-local.1",
  "private": true,
  "description": "Local fork of the official LLM service: a provider that names no retryPolicy falls back to the dsh-defaults namespace's defaultRetryCount (runtime global default for every adapter, not only pi-ai). Same package name so the profile node_modules copy overrides the global install.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./invariant": "./lib/invariant.js",
    "./src/*": "./src/*",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "lightningcss": "1.32.0",
    "tsdown": "0.22.2",
    "typescript": "~5.7.2"
  },
  "license": "MIT"
}
