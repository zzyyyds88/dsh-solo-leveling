{
  "name": "@deepseek-ai/dsh-llm-deepseek",
  "version": "0.1.0-rc.6-local.1",
  "private": true,
  "description": "Local fork of the official DeepSeek provider: the provider's retryPolicy falls back to the dsh-defaults namespace's defaultRetryCount when none is declared (runtime global default for the built-in provider too). Same package name so the profile node_modules copy overrides the global install.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./invariant": "./lib/invariant.js",
    "./src/*": "./src/*",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "lightningcss": "1.32.0",
    "tsdown": "0.22.2",
    "typescript": "~5.7.2"
  },
  "license": "MIT"
}
