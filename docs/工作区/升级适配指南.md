# DSH 升级适配指南（AI Agent 用）

> 面向**任意 AI Agent** 执行 DSH 官方升级时的强制规则与步骤。本仓库是 deepseek-harness 的 fork 整合包（自维护基线、不跟随官方更新）；官方（`@deepseek-ai/*`）升级时，本仓库的「同包名 fork」与「针对旧版限制的适配层」必须按本指南适配，否则会静默跑旧代码。

## 0. 一条铁律（每次升级先读这条）

**如果官方新版已经实现了与本仓库改动相同的功能 → 优先用官方，删掉/停用本仓库对应的 fork 或适配层。**

- 判断依据是**官方新版源码**，不是 release notes 的字面描述。
- 只有官方**没做、或官方做的方式不满足需求**时，才保留本仓库的 fork / 适配层。
- 保留时也必须把 fork **重 base 到新版官方源码**，不能继续用旧版源码。

## 1. 当前基线

| 项 | 值 |
|---|---|
| 基线 | deepseek-harness `dsh-v0.1.1-rc.2`（已平铺仓库根；上一基线 `dsh-v0.1.0-rc.8`，2026-08 完成重 base） |
| 所有 workspace 包版本 | `0.1.1-rc.2-local.1`（防 npm 安装被官方包覆盖） |
| 正式端口 | 默认 3080（`dsh web` 默认端口）；本机即唯一运行环境，无独立「正式实例」 |
| 验证实例 | 本地打包后全局安装直接起实例验收 |

## 2. 本仓库对 DSH 的全部改动清单（升级时逐项核对）

> 以下为 rc.2 现状（2026-08 升级核对：官方 0.1.1 未实现与下表任何补丁相同的功能，**无官方化项，全部重 base 保留**）。升级到新版本时逐行对照官方新版源码：已官方化 → 弃用 fork；未官方化 → 重 base 到新版源码。**本表是本仓库 fork/适配层映射的唯一维护处。**

### 2.0 fork 标记与升级自检约定（强制，2026-08-23 起生效）

每个保留的 fork 改动点，其源码处必须有一行含 **`Local fork`** 的注释（写明改了什么、为什么，可引用本指南条目）。这是「语义级定制」在整文件覆盖式重 base 下的唯一可靠锚点。

**升级重 base 完成、进入构建验证之前，必须先跑标记自检并把命中与 §2 清单逐项对照：**

```bash
grep -rn "Local fork" packages apps --include="*.ts" --include="*.tsx" --include="*.yml" | grep -v tests/
```

**清单项在命中里找不到锚点 = 该定制已丢失，先恢复再继续。** 仅靠 git diff 人眼核对「- 方向」不可靠：rc.2 重 base 因此静默丢了三处定制（见 §7），当时构建与单测全绿，谁都没发现。

高危单项自检（重 base 后逐条跑；新加/修改 fork 时同步维护本表）：

| 清单项 | 自检命令 | 期望结果 |
|---|---|---|
| ui-settings scope 恒 host（反向） | `grep -rn "isLoopback ? 'host' : 'memory'" packages/client/ui-settings/src` | **0 命中** |
| ui-settings scope 恒 host（正向） | `grep -rn "Local fork" packages/client/ui-settings/src/client/index.ts packages/client/ui-settings/src/client/settings-scope.ts` | 两文件各 ≥1 命中 |
| session-log-export 不装配 | `grep -n "session-log" packages/bundle/web-app/cordis.patch.yml` | 仅注释行 |
| picker-browse 默认工作目录 | `grep -c "configuredStartPath" packages/host/directory-picker-browse/src/index.ts` | ≥2 |
| profile-boot plain-node 跳过 watch | `grep -n "loader')?.internal !== undefined" apps/cli/src/profile-boot.ts` | ≥1 |
| web-app 默认 HTTPS/0.0.0.0 | `grep -n "?? '0\.0\.0\.0'" packages/bundle/web-app/cordis.patch.yml` | ≥1 |

### 2.1 同包名 fork（重 base 保留；官方升级后逐项核对）

| fork（包名） | 本仓库改动点 | rc.2 现状 |
|---|---|---|
| `dsh-host-webserver` | `registerGate` 登录门闸钩子 + TLS 处理（已并入官方新增的 index injections 机制） | 官方未见，**保留** |
| `dsh-client-connection` | `webAuthAuthed`：登录后放行全部特权方法 | 官方未见，**保留** |
| `dsh-host-apiproxy` | AUTH 错误脱敏（host 返回前抹掉 provider 回显的 AUTH 消息） | 官方未见，**保留** |
| `dsh-client-ui-settings` | settings scope 一律 `host`（远程可读写）。锚点：`src/client/index.ts` 镜像构造 + `settings-scope.ts` bind，两处均带 Local fork 注释 | 官方仍 `isLoopback?'host':'memory'`，**保留**（曾被 rc.2 重 base 静默还原为官方判定，2026-08-23 恢复） |
| `dsh-host-directory-picker-browse` | 无路径时读 `dsh-defaults.defaultWorkingDirectory`（`configuredStartPath()`；package.json 依赖 + tsconfig reference 同步声明） | 官方未见，**保留**（曾被 rc.2 重 base 整体删除，2026-08-23 恢复） |
| `dsh-llm` / `dsh-llm-deepseek` / `dsh-llm-pi-ai` | 未声明 retryPolicy 时读 `dsh-defaults.defaultRetryCount` 重试兜底 | 官方未见，**保留** |
| `dsh-llm-pi-ai` | 手声明的 OpenAI 兼容模型给默认思考档位（off/low/medium/high），第三方端点也有思考开关 | 官方未见，**保留** |
| `dsh-client-ui-directory-picker-browse` | 构建期注入默认路径 | 已被 `dsh-defaults` 运行时方案取代，**已弃用** |

### 2.2 直接改官方包源码的定制（非 fork 包）

- `apps/cli` profile-boot：HMR 降级（plain-node 生产安装跳过 watch——以 `ctx.get('loader')?.internal !== undefined` 门控 watcher 挂载；曾被 rc.2 重 base 删除门控，2026-08-23 恢复）。
- `packages/bundle/web-app`：默认 HTTPS / 0.0.0.0 监听、`cordis.patch.yml` 新增自研插件行、浏览器「文本打开」入口。
- translate 非空字符串处理。

### 2.3 已官方化弃用的 fork（官方 rc.8 已实现，勿再重打）

- llm 思考强度档位（`dsh-llm-deepseek` / `dsh-llm-pi-ai`）：官方已内置 → 弃用 fork，只保留重试兜底。
- 图像支持：官方 rc.8 已实现 → 弃用 fork。

### 2.3.1 应用要求不装配的上游功能（重 base 后必须保持移除）

- `dsh-session-log-export`（Session log 头部按钮 + `/export` 命令 + 下载弹窗）：
  **用户明确要求移除**。rc.2 重 base 时曾被上游装配悄悄带回（2026-08-23 发现并再次移除）——
  上游包源码保留在 `packages/session-query/session-log-export/`，但
  `packages/bundle/web-app/cordis.patch.yml` **不装配**、`package.json` 无依赖；
  每次升级重 base 后核对装配清单里不得再出现 `session-log-download` 行。

### 2.4 自研插件（第一方包，升级免疫）

门闸、默认值、手机端适配、桌宠、任务看板、实时统计、Git 图谱、右侧面板、图像理解、皮肤中心（maid-atelier）等自研包已整合进 harness 源码（`@deepseek-ai/dsh-*`），官方升级一般不影响；只有官方 client bundle 的加载格式变化时才需重建。清单见 [PLUGINS.md](../../PLUGINS.md)。

## 3. 升级适配流程（AI Agent 逐步执行）

1. **读官方 release notes + 源码 diff**（对照 `packages/` 新旧结构）。
2. **逐项核对 §2**：官方是否已实现同功能。
   - 已官方化 → **弃用**本仓库对应的 fork/适配层，改用官方。
   - 未官方化 → **重 base**：把对应包源码更新到新版官方源码 + 重打补丁。恢复/重打时每个改动点必须带 `Local fork` 注释（§2.0）。
3. **跑 §2.0 标记自检**：master grep 命中与 §2 清单逐项对照、高危单项 grep 逐条跑；任何一项缺锚点 = 定制丢失，必须先恢复。此步不过不得进入构建验证。
4. **本地验证**（打包后全局安装起实例逐项核对全绿；含远程浏览器场景——非 loopback 访问设置必须可用）。
5. **用户验收后**正式重装：`npm i -g ./dist/npm/*.tgz`（用户确认后执行）。
6. 同步更新本指南 §1 基线、AGENTS.md「关键事实」、PLUGINS.md。

## 4. 防静默过期

整合包自维护基线，DSH 官方升级后要重 base 到新版源码（§3），避免「DSH 已升级、整合包还是旧版源码」的静默问题。升级前先读 §0 铁律。

## 5. 回退

- 弃用 fork 前先 git 提交当前状态（可 revert）。
- 回退 = git revert + 重新 `pnpm run build` + 重新打包（用户执行）。

## 6. 升级记录（rc.8 → rc.2，2026-08）

- **结论**：逐项核对 §2，官方 0.1.1 未实现与任一 fork 补丁相同的功能——无官方化项，
  全部重 base 到 rc.2 源码保留；包版本统一 `0.1.1-rc.2-local.1`。
- **官方 0.1.1 主要变化**（影响后续定制开发的背景）：
  - 多模态附件链路：`attachment-local` 新增编码/归一化/请求图像处理，
    `llm-deepseek` 新增 files-api 文件上传（file-store / upload-index）。
  - 新增授权接缝 `packages/credentials/authorization`（`ctx.authorization`，
    插件主导的对话式凭据获取流程）。
  - 新增实验性 Agent Teams（`packages/experimental/agent-team`，见 `docs/subsystems/agent-team.md`）。
  - `llm-pi-ai` 新增登录/鉴权（auth / login）；`session-log-export` 新增头部操作；
    `ui-subagent` 新增谱系头；Web 端 markdown 宽表格支持。
  - 仓库层：新增 `BRAND_GUIDELINES`、`README.zh.md` / `CONTRIBUTING.zh.md` 双语文档对，
    CI 拆出 `ci-master` / `release-publish` / `release-vendor-publish` 工作流。
- **定制点复验**：默认 HTTPS / 0.0.0.0 监听、访问门闸（已并入官方 index injections 机制）、
  各设置卡（门禁/默认值/移动端适配等）需按红线 3 起实例逐项冒烟。

## 7. rc.2 重 base 定制丢失审计与恢复记录（2026-08-23）

rc.2 重 base（提交 `767711ad`，上游整文件覆盖）静默丢失了 **三处** 语义级定制，
全部于 2026-08-23 由 Linux 实机部署暴露并恢复。本节是 §2.0 标记自检约定的事实依据：

1. **`dsh-client-ui-settings` scope 恒 host**：`index.ts` 镜像构造与 `settings-scope.ts`
   bind 两处被还原成官方 `isLoopback ? 'host' : 'memory'` → 远程（非 loopback）浏览器
   settings 整体进入 memory 终态：提供方目录报「settings are unavailable in this
   browser」、插件配置页静默空白。原 fork 见 `c8c8d489`。
2. **`dsh-host-directory-picker-browse` 默认工作目录**：`configuredStartPath()` 方法、
   `@deepseek-ai/dsh-settings` import、package.json 依赖与 tsconfig reference 被整体删除。
3. **`apps/cli` profile-boot HMR 降级门控**：plain-node 生产安装跳过 watcher 的
   `loader.internal` 门控被删，watcher 变成无条件挂载。

**教训**：「构建全绿 + 单测全绿」证明不了定制还在——三处丢失时两者都是绿的；
实例冒烟当时只验了 loopback 场景，也发现不了第 1 处。重 base 后必须跑 §2.0
标记自检 + 远程浏览器场景实测。
