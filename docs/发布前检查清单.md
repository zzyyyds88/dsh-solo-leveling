# 发布前检查清单

> 把本仓库开源到 GitHub 之前，按顺序逐条打勾。**安全类必须做完再 push。**

## 一、安全（必须，先做）

- [ ] **轮换生产门闸口令**：旧口令已从 git 历史清除，但生产 `settings.yaml` 里仍是旧的，先改掉再开源。
- [ ] **删除清史备份**：`/tmp/dsh-backup-20260817-122302.bundle`（内含旧口令的历史，不在仓库里但仍在磁盘上）。
- [ ] **回退误装到生产的默认值插件**（早期 DSH_HOME bug 误写，非本仓库正式内容）：
      `dsh plugin --profile web remove dsh-defaults dsh-client-ui-defaults`

## 二、生产环境收尾（可选，升级到最新插件）

- [ ] **重装 access-gate**：拿到「会话 Cookie 名按端口派生」修复（`dsh_session_3080` 与测试实例 `dsh_session_3090` 不再互相挤下线）。
- [ ] **理清全局安装包里的旧补丁**：`/usr/lib/node_modules/@deepseek-ai/dsh` 里的 webserver 曾直接改过（`registerGate` 带 `Local patch` 注释），决定保留还是统一改走 profile fork 覆盖。

## 三、内容

- [ ] README 截图补齐：`docs/screenshots/02-accessgate-card.png`（访问门禁卡片）、
      `03-defaults-card.png`（默认值卡片）、`04-pet.png`（桌宠）、`05-taskboard.png`（任务看板）。
- [ ] 各项目 README 通读一遍，确认无过期信息、无指向已删除文件的链接。

## 四、发布

- [ ] 在 GitHub 建同名空仓库 `dsh-solo-leveling`（不要勾选初始化 README，避免历史冲突）。
- [ ] `git push --force-with-lease origin main`（历史已用 filter-repo 重写过，必须强推）。
- [ ] 仓库描述 + Topic 标签：`dsh`、`dsh-plugin`、`deepseek-harness`、`plugin`。
- [ ] 确认根 LICENSE（Apache-2.0）与各包 `package.json` 的 license 一致；maid-atelier（CC BY-NC-SA 4.0）与 miku（BSD）署名保留。

## 五、（可选）进生态 / 转 npm 分发

- [ ] 登记进 [awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin)
      或 [dsh-suite](https://github.com/whyihaveyou/dsh-suite) 目录。
- [ ] 上游化 PR：把 `registerGate` / 命名空间暴露注册 / 远程设置放行等扩展点提交进
      deepseek-harness 官方；合入后 fork 归零，access-gate / defaults 即可纯 npm 分发。
