# 整合迁移路线图（大宝贝定制版）

> 目标：把 deepseek-harness `dsh-v0.1.0-rc.7` 平铺为仓库根基线，再把 5 个插件项目
> （含 8 个 `@deepseek-ai/*` 同名 fork）整合进 harness 源码，整体构建成「大宝贝定制版」。
> 本文件是逐项映射 + 迁移顺序 + 验证口径。迁移前先读 [升级适配指南.md](升级适配指南.md) §0 铁律：
> **官方新版已实现同功能 → 优先用官方，弃用对应 fork/适配层。**

## 0. 基线

| 项 | 值 |
|---|---|
| 上游仓库 | `deepseek-ai/deepseek-harness` |
| 基线 tag / commit | `dsh-v0.1.0-rc.7` = `99f6f02fecdb7dff40c3fbc9470f5907c29f74ca` |
| 整合前快照 | git commit `500c85e`（现有插件工作树） |
| 仓库结构 | harness 源码已平铺仓库根（`packages/<group>/<pkg>/`，包名 `@deepseek-ai/dsh-<pkg>`） |

## 1. 八个同名 fork → rc.7 落点

> rc.7 已重组为域目录；迁移 = 把 rc.6 的 fork diff **重 base 到 rc.7 源码**（对照各项目
> `定制记录/本次diff.patch`），版本号 `<rc.7>-local.1`。先核对「rc.7 是否已官方化」再动手。

| fork 包名（rc.6） | 本仓库改动点 | rc.7 落点 | 官方化核对 |
|---|---|---|---|
| `dsh-host-webserver` | `registerGate` 登录门闸钩子 | `packages/host/webserver` | 未见，保留；✅ 已重 base |
| `dsh-host-apiproxy` | `exposedNamespaces()` 加 `web-auth`/`access-gate`/`dsh-defaults`/task-suite 命名空间 | `packages/host/apiproxy` | rc.7 `settings.describe()` 动态暴露全部命名空间 → **弃用，无需重 base** ✅ |
| `dsh-client-connection` | `webAuthAuthed` 登录后放行特权方法 | `packages/client/connection` | 未见，保留；✅ 已重 base |
| `dsh-client-ui-settings` | settings scope 一律 `host`（远程可读写） | `packages/client/ui-settings` | rc.7 仍 `isLoopback?'host':'memory'` → 保留；✅ 已重 base（恒 host） |
| `dsh-host-directory-picker-browse` | 无路径时读 `dsh-defaults.defaultWorkingDirectory` | `packages/host/directory-picker-browse` | 未见，保留；✅ 已重 base（2026-08-18） |
| `dsh-llm` | 未声明 retryPolicy 的 adapter 读 `dsh-defaults.defaultRetryCount` | `packages/llm/llm` | 未见，保留；✅ 已重 base（prepareRoutes 兜底） |
| `dsh-llm-deepseek` | DeepSeek 重试兜底 + 思考强度档位 | `packages/llm/llm-deepseek` | rc.7 已给 DeepSeek off/low/high/max（思考强度**官方更全→弃用**）；重试兜底 ✅ 已重 base |
| `dsh-llm-pi-ai` | 第三方手写模型思考档位 off/low/medium/high + 重试兜底 | `packages/llm/llm-pi-ai` | rc.7 已有 THINKING_LEVELS/reasoningEfforts（思考档位**官方化→弃用**）；重试兜底 ✅ 已重 base |
| `dsh-client-ui-directory-picker-browse` | 构建期注入 `DSH_PICKER_DEFAULT_PATH` 作为起始目录 | `packages/client/ui-directory-picker-browse` | host 端已读 `dsh-defaults.defaultWorkingDirectory`（settings 运行时）→ **弃用，无需重 base** ✅ |

## 2. 纯插件 → rc.7 落点

> 纯插件不依赖改官方包，本身就是标准插件（`dsh plugin add` 可装）。整合包目标「一装全有」，
> 故迁入 harness `packages/<group>/<pkg>/` 并按官方分组规范命名（`@deepseek-ai/dsh-*`）。

| 插件 | 类型 | 建议落点（组） |
|---|---|---|
| `dsh-host-access-gate` | host 后端 | `packages/host/access-gate` ✅ 已迁移（939 行 JS→TS，注册进 web-app；caddy 二进制留待后续） |
| `dsh-client-ui-access-gate` | client 前端 | `packages/client/ui-access-gate` |
| `dsh-defaults` | host 后端 | `packages/settings/defaults` ✅ 已迁移（2026-08-18，构建+lint 全绿） |
| `dsh-client-ui-defaults` | client 前端 | `packages/client/ui-defaults` |
| `dsh-mobile-adapt` | client 前端 | `packages/client/ui-mobile-adapt` ✅ 已迁移（2026-08-18，构建+lint 全绿） |
| `deepseek-pet` | client 前端 | `packages/client/ui-pet` 🔄 迁移中（骨架+host 已提交，client ~2153 行 JS/JSX→TS 转换中） |
| `dsh-task-suite`（`@zzyyyds88/*` 8 包） | 混合 | 前端进 `packages/client/ui-*`，host 服务进 `packages/host/*` |

task-suite 拆分明细：task-board / live-stats / git-graph / aionui-panel / describe-image /
web-ui-settings / skin-center → client；skin-center 的 host 半（`/api/skin-center/*` 热切换）
与 git-graph 的 host git 服务 → host。

> task-suite 迁移要点（2026-08-18 调研）：① 已是 TS 无需类型转换，核心是
> `@zzyyyds88/*` → `@deepseek-ai/dsh-*` scope 改名 + rc.6→rc.7 API 重 base + 装配注册；
> ② 共享代码 `shared/`（`PluginSettingsCard`/`settings-form`/`dsh-home`/`poll-guard`）由
> `scripts/sync-shared.mjs` 复制进各包（非包名导入），迁入需决定「抽共享包 vs 继续复制」；
> ③ `scripts/aggregate.mjs` 生成 `dsh-task-suite-all` 的 cordis.patch.yml（聚合行），迁入后
> 聚合包可退役、各行直接在 web-app/base 注册；④ 皮肤（maid-atelier）资产 + skin-center
> host 半（`/api/skin-center/*` 热切换）单独处理；⑤ 上游 Apache-2.0 + CC BY-NC-SA 许可随包保留。

## 3. 关键架构问题（迁移第一步先解决）

1. **bake-in 装配点（已定位）**：官方首方插件的装配是
   `packages/bundle/base/cordis.patch.yml`（dsh-base，每个 profile 先应用，451 行 `- insert:` 行）+
   `packages/bundle/web-app/cordis.patch.yml`（web 模式补丁：host 行 + 浏览器 roster 的
   `dsh.client` 行）+ 用户 profile 层。要让自研插件「一装全有」：
   - **host 端**插件（门闸 / defaults / describe-image 等）→ 在 base 或 web-app 的
     `- insert:` 列表加一行 `{id, name}`；
   - **client 端**插件（桌宠 / 手机端 / task-board 等）→ 在 web-app 的「browser plugin roster」
     （`dsh.client` 行）加行。
   - **8 个 fork 不加行**，直接改现有行对应包源码（§1 落点）。
2. **命名/ctx-key**：每个组 README 维护「package/ctx-key 映射」，迁入后需同步更新
   `packages/<group>/README.md` 与 `packages/README.md` 层级表。
3. **客户端 bundle 纯度门**：client 插件 bundle 只能 require 平台表 + INLINE_SAFE 白名单
   （参照 `packages/client` 现有 tsdown 预设），迁入后需过构建纯度门。

> 已核对：rc.7 把重试**执行**拆到官方 `@deepseek-ai/dsh-llm-retry`（base 装配 `llm-retry` 行，
> 监听 `agent/request-error` 执行退避），但**策略解析仍在各 adapter 内**：`resolveRetryPolicy(undefined)`
> 返回默认 maxRetries=2（非 undefined），所以 deepseek/pi-ai 各自在 `resolveAdapterOptions` 解析策略、
> 绕过 `dsh-llm` prepareRoutes 的通用兜底——重试兜底 fork 需在**三层**分别重 base
> （`dsh-llm` prepareRoutes + `dsh-llm-deepseek`/`dsh-llm-pi-ai` 各自的 resolveAdapterOptions）。

## 4. 迁移顺序

1. 纯插件（自包含、不碰官方包）：先 `dsh-mobile-adapt` / `deepseek-pet`（前端，改完刷新即生效）→
   `dsh-defaults` + `dsh-client-ui-defaults` → 门闸（host + client）→ task-suite 拆分。
2. 8 个同名 fork（重 base rc.7 源码）：先核对官方化，逐个改 `packages/<group>/<pkg>/src`，
   每个 fork 迁移后 `pnpm run build` + test-env 验证。
3. 装配：把迁入的插件注册进产品装配（preset / composition），确保 `pnpm dsh web` 默认带齐。

## 5. 验证口径

- `pnpm install && pnpm run build` 全绿；
- 测试环境 `scripts/test-env-start.sh`（端口 3090）逐项实测（见各项目 `verify.sh`）；
- **生产环境（3080 / `$HOME/.dsh` / 全局安装）绝不触碰**，正式安装由用户在 SSH 终端执行。

## 6. 回退与安全

- 整合前快照 `500c85e`；迁移每一步单独 commit，可逐步 revert。
- 上游原文存档：`AGENTS.upstream.md` / `CONTRIBUTING.upstream.md` / `README.upstream.md` /
  `README.zh.md` / `CONTRIBUTING.zh.md`。
- 仓库 git 远端仍为 `dsh-solo-leveling`；对外发布前再定是否重命名（如 `dabao-custom`）。

## 7. 开放项

- 仓库重命名 / GitHub 远端建仓（发布时再定）。
- 迁移完成后退役 `dsh-AccessGate/`、`dsh-Moresettings/`、`dsh-deepseekpet/`、
  `dsh-mobile/`、`dsh-task-suite/` 五个旧目录（归档，不双维护）。
- **参数配置化铁律**：今后新增的任何插件，可调参数必须进「设置 → 插件 → 插件配置」卡片、
  禁止硬编码（见 AGENTS.md 红线 8 / CONTRIBUTING.md 铁律 8）。
  合规核查：`dsh-defaults`（设置命名空间 + 卡片）与三个重试 fork（读 `dsh-defaults`）已合规；
  `dsh-mobile-adapt` 的断点 768px / 抽屉宽 360px / 桌宠缩放 0.75 目前硬编码在 CSS 中，
  需补「手机端适配」设置卡暴露这些参数（迁移收尾时处理）。

## 8. 验收与打包流程（完成定义）

> 顺序固定：**我测 → 打包 → 用户测 → push GitHub**。用户测试通过前**禁止 push GitHub**。

1. **我测（验收）**：全量迁移完成后，`pnpm install && pnpm run build` 构建全绿；
   在 `test-envs/`（端口 3090~3093）逐项实测（各插件 verify + 门闸/桌宠/右侧面板/
   任务套件/图像理解等），结果写进 `docs/opencode-实测反馈.md`。
2. **打包成 npm 包（同官方用法）**：官方 CLI 包 = `apps/cli`（包名 `@deepseek-ai/dsh`，
   `bin.dsh = lib/bin.js`，`npx @deepseek-ai/dsh web` 即 `dsh web`）。构建后
   `pnpm --filter @deepseek-ai/dsh pack`（或 `npm pack`）产出 tarball。
   scope 约束：`@deepseek-ai` 归官方所有、无法发布 → 正式发布需换个人 scope
   （如 `@zzyyyds88/dsh`，bin 仍叫 `dsh`）；本地给用户测试可先 tarball 全局安装
   （`npm i -g ./<tarball>` → `dsh web`），体验与 `npx @deepseek-ai/dsh web` 一致。
3. **用户测**：用户按官方方式安装/运行自测。
4. **push GitHub**：仅在用户测试通过后执行（远端/仓库名届时再定）。
